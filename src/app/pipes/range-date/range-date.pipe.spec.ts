import { RangeDatePipe } from './range-date.pipe';

describe('RangeDatePipe', () => {
  it('create an instance', () => {
    const pipe = new RangeDatePipe();
    expect(pipe).toBeTruthy();
  });
});
